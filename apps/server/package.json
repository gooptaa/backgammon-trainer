{
  "name": "@backgammon-trainer/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/index.js",
    "lint": "eslint src test --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@backgammon-trainer/ai-contracts": "workspace:*",
    "@backgammon-trainer/backgammon-analysis": "workspace:*",
    "@backgammon-trainer/shared": "workspace:*",
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "tsx": "^4.19.1"
  }
}
